\documentclass[11pt]{article}
\usepackage[english]{babel}
\usepackage[utf8x]{inputenc}
\usepackage[T1]{fontenc}

%% Sets page size and margins
\usepackage[letterpaper, portrait, margin=1in,top=1in,bottom=1.5in]{geometry}

%% Useful packages
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{amsthm}
\usepackage{amsfonts}
\usepackage{mathrsfs}
\usepackage{tikz}
\usepackage{graphicx}
\usepackage[shortlabels]{enumitem}

\newenvironment{exercise}[1]{\textbf{#1.}}

\begin{document}

\begin{flushright}
Cory Glover\\
2/6/20\\
Math 525
\end{flushright}

\begin{center}
\textbf{HW 12}
\end{center}

\begin{exercise}{8.1}
\begin{enumerate}
\item $O(n^2)$
\item $O(n)$
\end{enumerate}
\end{exercise}

\begin{exercise}{8.3}
\begin{enumerate}
\item $O(n^2)$
\item $O(n)$ since there are $m$ multiplications and $m$ additions and $m\propto n$.
\item Note that $Bv=Av-\frac{1}{2m}Kv$ where $K_{ij}=k_ik_j$. We also note that $K_iv=k_ik_1v_1+k_ik_2v_2+\dotsb+k_ik_nv_n=k_i(\sum_{j=1}^nk_jv_j)$ where $K_i$ is the $i^{th}$ row of $K$. We then devise the following algorithm:
\begin{enumerate}
\item Compute $Av$.
\item Calculate each $k_i$.
\item Calculate $\sum_{i=1}^nk_iv_i$.
\item Calculate each $k_i(\sum_{j=1}^nk_jv_j)$.
\item Add $(Av)_i$ to $k_i(\sum_{j=1}^nk_jv_j)$ for each $i$.
\end{enumerate}
We analyze each step. From part (2) we know that step one has complexity $O(n)$. The second step has complexity $O(m/n)$ for each node, so in total has complexity $O(n)$. The third step has $n$ multiplications and $n$ additions and is thus $O(n)$. The fourth step is $n$ multiplications so it has $O(n)$. The last step has $n$ additions so it is $O(n)$. So overall, the algorithm has complexity $O(n)$.
\end{enumerate}
\end{exercise}
\end{document}